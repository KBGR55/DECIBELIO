name: 03 SCA

on:
  workflow_dispatch:
  workflow_call:

jobs:
  sca:
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-22.04"]
        manifest_file: ["pom.xml"]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    name: SCA Java project
    steps:
      - name: Checkout action
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Needed for CodeQL to see full history

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Install SCA tool (CRDA)
        uses: redhat-actions/openshift-tools-installer@v1
        with:
          source: github
          github_pat: ${{ github.token }}
          crda: "latest"

      - name: Run Static Code Analysis (SCA)
        id: scan_java
        run: |
          cd decibelio
          mvn clean install

      # OWASP Dependency Check
      - name: Run OWASP Dependency-Check
        run: |
          cd decibelio
          curl -sSL https://github.com/jeremylong/DependencyCheck/releases/latest/download/dependency-check-8.3.1-release.zip -o dependency-check.zip
          unzip dependency-check.zip
          ./dependency-check/bin/dependency-check.sh --project "decibelio" --scan . --format HTML --out ../dependency-check-report.html
        continue-on-error: true

      # Static analysis tools
      - name: Run SpotBugs
        run: |
          cd decibelio
          mvn com.github.spotbugs:spotbugs-maven-plugin:spotbugs
          mvn com.github.spotbugs:spotbugs-maven-plugin:spotbugs -Dspotbugs.outputFileType=html

      - name: Run PMD
        run: |
          cd decibelio
          mvn org.apache.maven.plugins:maven-pmd-plugin:pmd
          cp target/pmd.xml ../pmd-report.xml || echo "PMD report not found"

      - name: Run Checkstyle
        run: |
          cd decibelio
          mvn org.apache.maven.plugins:maven-checkstyle-plugin:checkstyle

      # Semgrep analysis
      - name: Run Semgrep
        uses: returntocorp/semgrep-action@v1

      # CodeQL analysis - fixed version
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: java
          queries: +security-and-quality

      - name: Build with Maven (for CodeQL)
        run: |
          cd decibelio
          mvn clean install -DskipTests

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2

      # Artifacts upload for reports
      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sca-reports
          path: |
            decibelio/target/spotbugs.html
            decibelio/target/site/spotbugs.html
            pmd-report.xml
            decibelio/target/checkstyle-result.xml
            dependency-check-report.html
          retention-days: 5